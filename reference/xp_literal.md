# Add literal text to a pattern

Add literal text to a pattern

## Usage

``` r
xp_literal(pattern = NULL, literal = NULL)
```

## Arguments

- pattern:

  (Optional) An existing regex string from the pipe.

- literal:

  The exact text/symbols you want to add.

## Value

A character string.
